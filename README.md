# 

paas 平台后后端服务，用于开发者管理游戏，采购商购买并部署对接游戏。

## 快速开始

1. 准备配置文件

   * 复制 `config.yaml.example` 为 `config.yaml`
   * 修改配置文件中的配置为实际使用的配置
  
2. 初始化数据库，使用根目录下的paas.sql文件初始化数据库
  
3. 编译并运行

```shell
make build && ./engine
```
# go-api-template
